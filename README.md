# Power Network Visualisation

## Installation

    sudo apt-get install npm
    npm install -g bower
    bower install

## Running

    # localhost:8000
    python -m http.server #python3
    python -m SimpleHTTPServer #python2
    # also works on apache, nginx...
